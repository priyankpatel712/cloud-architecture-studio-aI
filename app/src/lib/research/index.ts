// Research layer (feature 008) — web-search backends and the store -> MCP -> web
// knowledge waterfall. Optional: with no search credential configured the web
// rung is inert and generation proceeds on remaining sources (FR-027).
export {};
