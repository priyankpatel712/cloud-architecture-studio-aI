// Knowledge layer (feature 008) — provider-agnostic storage and retrieval of
// reusable generation knowledge. Provider-specific rules live in
// src/lib/providers/<id>/rules.ts (constitution Principle II), not here.
export {};
